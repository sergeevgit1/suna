#############################################
# Пример переменных окружения для фронтенда #
# (Next.js, папка frontend)                 #
#############################################

# Как использовать этот файл:
# 1) Локально: скопируйте в `.env.local` и заполните значениями.
#    Пример: `cp frontend/.env.example frontend/.env.local`.
# 2) Dokploy/Production: файлы `.env` не используются. 
#    Все переменные задаются через Dokploy Environments. Этот файл — документация.

# Важно про безопасность:
# - Все переменные с префиксом `NEXT_PUBLIC_` попадают в бандл и видны в браузере —
#   не храните там секреты.
# - `KORTIX_ADMIN_API_KEY` БЕЗ префикса `NEXT_PUBLIC_` и должен использоваться
#   только на серверной стороне (server actions, маршруты, и т.п.).

# Конвенции URL:
# - `NEXT_PUBLIC_BACKEND_URL` обычно содержит `/api` на конце основания,
#   например: `http://localhost:8000/api` или `https://api.example.com/api`.
#   Не ставьте завершающий слеш (`/`) в конце.
# - `NEXT_PUBLIC_URL` — публичный адрес фронтенда, без завершающего слеша.

# Типичные значения для локальной разработки:
# - NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# - NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
# - NEXT_PUBLIC_BACKEND_URL=http://localhost:8000/api
# - NEXT_PUBLIC_URL=http://localhost:3000

# Типичные значения для Dokploy/Production:
# - NEXT_PUBLIC_SUPABASE_URL=https://<your>.supabase.co
# - NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
# - NEXT_PUBLIC_BACKEND_URL=https://<backend-domain>/api
# - NEXT_PUBLIC_URL=https://<frontend-domain>

########################################
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (минимум для run)
########################################

NEXT_PUBLIC_ENV_MODE=LOCAL                # Режим окружения: LOCAL | staging | production
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321    # Supabase URL (локально: 127.0.0.1:54321)
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_example # Supabase anon key (publishable)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000/api   # База API бэкенда (<origin>/api, без завершающего /)
NEXT_PUBLIC_URL=http://localhost:3000               # Публичный URL фронтенда

#############################################
# НЕОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (с безопасными заглушками)
#############################################

NEXT_PUBLIC_APP_URL=http://localhost:3000           # Хелпер-URL для метаданных; необязателен
NEXT_PUBLIC_GOOGLE_CLIENT_ID=google-client-id-placeholder      # OAuth Client ID; необязателен
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=verification-token-placeholder # Search Console; необязателен
NEXT_PUBLIC_POSTHOG_KEY=phc_placeholder              # Аналитика; необязателен
NEXT_PUBLIC_SENTRY_DSN=https://example.com/abc       # Мониторинг ошибок; необязателен
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=stripe_pk_test_placeholder  # Оплата; необязателен
NEXT_PUBLIC_TOLT_REFERRAL_ID=tolt-referral-placeholder          # Рефералы; необязателен
NEXT_PUBLIC_PHONE_NUMBER_MANDATORY=false            # true/false — требовать телефон при MFA; необязателен
NEXT_PUBLIC_VERCEL_ENV=production                   # Окружение Vercel; необязателен

# Серверные (НЕ публичные) переменные
KORTIX_ADMIN_API_KEY=changeme_admin_key             # Приватный админ-ключ для server actions; необязателен
EDGE_CONFIG=https://edge-config.vercel.com/placeholder?token=placeholder # Edge Config URL; необязателен
