# Исследование бесплатных self-hosted Sandbox решений для SUNA

**Дата:** 13 ноября 2025  
**Цель:** Найти бесплатное open-source решение для замены Daytona, которое можно развернуть на собственном сервере

---

## Обзор исследованных решений

### 1. **SandboxAI от Substratus** ⭐ РЕКОМЕНДУЕТСЯ

**GitHub:** https://github.com/substratusai/sandboxai  
**Stars:** 121 ⭐  
**License:** Open Source  
**Язык:** Go (48.5%), Python (44.8%)

#### Описание
SandboxAI - это open source runtime для безопасного выполнения AI-generated Python кода и shell команд в изолированных sandbox контейнерах.

#### Ключевые особенности

✅ **Self-host**
- Docker-based single host backend (готово)
- Kubernetes-based multi-node backend (coming soon)

✅ **Simple SDK**
- `pip install sandboxai-client` - это все, что нужно
- Go SDK included
- Простой Python API

✅ **Container based**
- Вы определяете окружение через Dockerfile
- Интеграция с существующими инструментами
- Используется Ubuntu LTS

✅ **Flexible control**
- **Full-agency** - дать агенту полную свободу внутри контейнера
- **Limited-agency** - дать агенту доступ к конкретным командам

✅ **Open API**
- Simple RESTful interface
- Можно генерировать свой собственный клиент из API spec

#### Архитектура

```
Agent Orchestration (CrewAI, LangGraph, etc.)
           ↓
      Inference (LLM)
           ↓
    Tool Execution
           ↓
       SandboxAI  ←  Hardcoded Funcs
           ↓
   Create sandboxes on demand
           ↓
         boxd (daemon)
      /         \
ipython kernel   bash shell
      \         /
   Sandbox Container
```

#### Quickstart пример

```python
from sandboxai import Sandbox

# Embedded mode - запускает sandbox server локально
with Sandbox(embedded=True) as box:
    print(box.run_ipython_cell("print('hi')").output)
    print(box.run_shell_command("ls /").output)
```

#### Deployment

**Single-host (Docker):**
```bash
# Установить docker
docker ps

# Установить Python client
pip install sandboxai-client

# Использовать в коде
python your_agent.py
```

**Multi-node (Kubernetes):** Coming soon

#### Преимущества для SUNA

1. **Минимальные изменения кода** - похожий API на Daytona
2. **Бесплатно и open-source** - нет vendor lock-in
3. **Простая интеграция** - Python SDK готов к использованию
4. **Гибкая настройка** - можно кастомизировать Docker image
5. **Активная разработка** - последний commit 9 месяцев назад
6. **Поддержка AI agents** - специально разработан для AI use cases

#### Недостатки

1. **Молодой проект** - только 121 stars, может быть нестабильным
2. **Нет browser support** - только Python и shell
3. **Нет file upload UI** - нужно реализовывать самостоятельно
4. **Ограниченная документация** - только README

---

### 2. **LemonAI Sandbox**

**GitHub:** https://github.com/hexdocom/lemonai  
**Stars:** 1.3K ⭐  
**License:** Open Source  
**Язык:** TypeScript (51.6%), JavaScript (47.0%)

#### Описание
LemonAI - это Full-stack Open-source Self-Evolving General AI Agent, альтернатива Manus. Включает встроенный Code Interpreter VM sandbox.

#### Ключевые особенности

✅ **Local VM Sandbox**
- Работает внутри локальной Virtual Machine (VM) sandbox через Docker
- Защищает файлы и ОС машины
- Безопасно обрабатывает написание, выполнение и редактирование кода

✅ **Docker-based**
- Использует Docker Compose для развертывания
- Монтирует `/var/run/docker.sock` для управления контейнерами
- Создает отдельные контейнеры для каждого пользователя

✅ **Full-stack решение**
- Frontend (Vue3)
- Backend (Node.js)
- Browser server
- Runtime sandbox

#### Архитектура

**Docker Compose структура:**
```yaml
services:
  lemon:
    image: hexdolemonai/lemon:latest
    environment:
      - DOCKER_HOST_ADDR=host.docker.internal
      - ACTUAL_HOST_WORKSPACE_PATH=${WORKSPACE_BASE:-$PWD/workspace}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # Управление Docker
      - ${WORKSPACE_BASE:-$PWD/workspace}:/app/workspace
      - ${WORKSPACE_BASE:-$PWD/data}:/app/data
```

**Runtime архитектура:**
- `DockerRuntime.js` - основной класс для управления sandbox
- Использует `dockerode` для управления Docker API
- Создает контейнеры с именем `user-{user_id}-lemon-runtime-sandbox`
- Поддерживает persistent workspace для каждого пользователя

#### Как работает sandbox

1. **Инициализация контейнера:**
```javascript
async init_container() {
  const request = {
    method: 'POST',
    url: `${ECI_SERVER_HOST}/amd`,
    data: { 
      name: `user-${this.user_id}-lemon-runtime-sandbox`, 
      workspace: `user_${this.user_id}` 
    },
  };
  const response = await axios(request);
  this.docker_host = response.data.IntranetIp;
}
```

2. **Выполнение действий:**
```javascript
async execute_action(action, context = {}, task_id) {
  switch (type) {
    case 'write_code':
      result = await this.write_code(action, uuid);
      break;
    case 'terminal_run':
      result = await this._call_docker_action(action, uuid);
      break;
    case 'read_file':
      result = await this.read_file(action, uuid);
      break;
    case 'browser':
      result = await this._call_docker_action(action, uuid);
      break;
  }
}
```

3. **Вызов Docker API:**
```javascript
async _call_docker_action(action, uuid) {
  const host = this.docker_host;
  const request = {
    method: 'POST',
    url: `http://${host}:${this.host_port}/execute_action`,
    data: { action: action, uuid: uuid },
  };
  const response = await axios(request);
  return response.data.data;
}
```

#### Преимущества для SUNA

1. **Полноценный пример** - можно изучить реальную реализацию
2. **Похожая архитектура** - Node.js backend, похож на SUNA
3. **Browser support** - есть интеграция с браузером
4. **File management** - работа с файлами и workspace
5. **User isolation** - отдельный контейнер для каждого пользователя
6. **Активная разработка** - последний commit вчера

#### Недостатки

1. **Сложная архитектура** - много компонентов
2. **Зависимость от ECI Server** - использует внешний сервис для управления контейнерами
3. **Китайский код** - комментарии на китайском языке
4. **Не standalone** - нужно разбираться в архитектуре

---

### 3. **Judge0**

**GitHub:** https://github.com/judge0/judge0  
**Stars:** 3.6K ⭐  
**License:** GPL-3.0  
**Язык:** Ruby

#### Описание
Judge0 - это robust, scalable, и open-source online code execution system. Используется для competitive programming, e-learning, candidate assessment.

#### Ключевые особенности

✅ **Production-ready**
- Используется в production на многих платформах
- Поддержка 75+ языков программирования
- RESTful API

✅ **Scalable**
- Kubernetes deployment
- Horizontal scaling
- Queue-based architecture

✅ **Secure**
- Использует `isolate` sandbox (Linux namespaces, cgroups)
- Resource limits (CPU, memory, time)
- Network isolation

#### Архитектура

- **API Server** (Ruby on Rails) - принимает submissions
- **Workers** - выполняют код в sandbox
- **Redis** - очередь задач
- **PostgreSQL** - хранение submissions

#### Преимущества

1. **Production-ready** - проверено временем
2. **Масштабируемость** - горизонтальное масштабирование
3. **Безопасность** - использует Linux namespaces
4. **Поддержка многих языков** - 75+ языков

#### Недостатки для SUNA

1. **Только code execution** - нет file management, browser, etc.
2. **Ruby on Rails** - другой стек технологий
3. **Сложная архитектура** - много компонентов
4. **Overkill** - слишком много функций для AI agent

---

### 4. **Microsandbox**

**GitHub:** https://github.com/zerocore-ai/microsandbox  
**Stars:** ~100 ⭐ (из предыдущего исследования)  
**License:** Open Source  
**Язык:** Rust

#### Описание
Microsandbox - это lightweight sandbox для выполнения untrusted code в micro VMs.

#### Ключевые особенности

✅ **Fast startup**
- Milliseconds startup time
- Micro VMs (Firecracker)

✅ **Secure**
- VM-level isolation
- Resource limits

✅ **Self-hosted**
- Полностью self-hosted
- Нет внешних зависимостей

#### Преимущества

1. **Очень быстрый** - milliseconds startup
2. **Безопасный** - VM-level isolation
3. **Бесплатный** - полностью open-source

#### Недостатки для SUNA

1. **Ранняя стадия** - молодой проект
2. **Только code execution** - нет file management, browser
3. **Rust** - другой язык программирования
4. **Требует Firecracker** - дополнительная инфраструктура

---

## Сравнительная таблица

| Критерий | SandboxAI | LemonAI | Judge0 | Microsandbox |
|----------|-----------|---------|--------|--------------|
| **Стоимость** | Бесплатно | Бесплатно | Бесплатно | Бесплатно |
| **Self-hosted** | ✅ | ✅ | ✅ | ✅ |
| **Простота интеграции** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| **Python SDK** | ✅ | ❌ | ❌ | ❌ |
| **Node.js SDK** | ❌ | ✅ | ❌ | ❌ |
| **File management** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ | ❌ |
| **Browser support** | ❌ | ✅ | ❌ | ❌ |
| **Code execution** | ✅ | ✅ | ✅ | ✅ |
| **Shell commands** | ✅ | ✅ | ❌ | ❌ |
| **Docker-based** | ✅ | ✅ | ✅ | ❌ (Firecracker) |
| **Kubernetes support** | 🔜 | ❌ | ✅ | ❌ |
| **Документация** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **Активность проекта** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Production-ready** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **Время внедрения** | 2-3 недели | 4-6 недель | 6-8 недель | 3-4 недели |

---

## Рекомендация для SUNA

### Вариант 1: SandboxAI (РЕКОМЕНДУЕТСЯ для быстрого старта)

**Почему:**
1. **Минимальное время внедрения** - 2-3 недели
2. **Простой API** - похож на Daytona, легко адаптировать
3. **Бесплатно и open-source** - нет vendor lock-in
4. **Специально для AI agents** - разработан для AI use cases
5. **Docker-based** - простое развертывание

**План внедрения:**

**Неделя 1: Proof of Concept**
- Установить SandboxAI локально
- Разработать адаптер для SUNA
- Протестировать базовые функции (write_code, terminal_run, read_file)

**Неделя 2: Интеграция**
- Интегрировать адаптер в SUNA backend
- Мигрировать инструменты (Files, Shell)
- Добавить error handling

**Неделя 3: Тестирование и деплой**
- A/B тестирование (10% → 50% → 100%)
- Мониторинг метрик
- Decommission Daytona

**Недостающие функции:**
- Browser support - нужно реализовать отдельно (можно использовать Playwright в sandbox)
- File upload UI - реализовать через API
- Persistent storage - настроить Docker volumes

---

### Вариант 2: Custom решение на основе LemonAI (для долгосрочной перспективы)

**Почему:**
1. **Полный контроль** - можно адаптировать под свои нужды
2. **Похожая архитектура** - Node.js backend
3. **Browser support** - уже реализован
4. **File management** - полноценная реализация
5. **Можно изучить код** - open-source

**План внедрения:**

**Неделя 1-2: Изучение кода LemonAI**
- Клонировать репозиторий
- Изучить `DockerRuntime.js`
- Понять архитектуру sandbox

**Неделя 3-4: Разработка собственного Runtime**
- Создать `SunaRuntime.js` на основе LemonAI
- Адаптировать под SUNA API
- Убрать зависимость от ECI Server

**Неделя 5-6: Интеграция и тестирование**
- Интегрировать в SUNA backend
- Мигрировать все инструменты
- Тестирование

**Преимущества:**
- Полный контроль над кодом
- Можно добавить любые функции
- Нет зависимости от внешних проектов

**Недостатки:**
- Больше времени на разработку (4-6 недель)
- Нужно поддерживать самостоятельно
- Сложнее отлаживать

---

## Следующие шаги

### Немедленно (эта неделя)

1. **Протестировать SandboxAI**
   ```bash
   # На вашем dev сервере
   pip install sandboxai-client
   python test_sandboxai.py
   ```

2. **Создать PoC адаптер**
   - Создать `backend/core/sandbox/sandboxai_adapter.py`
   - Реализовать базовые методы (run_code, run_shell, read_file)
   - Протестировать совместимость с текущим API

3. **Оценить feasibility**
   - Проверить, работает ли SandboxAI на вашем сервере
   - Оценить производительность
   - Принять решение: SandboxAI или Custom

### Следующая неделя

1. **Если SandboxAI:**
   - Начать интеграцию в SUNA
   - Разработать полноценный адаптер
   - Добавить недостающие функции (browser, upload)

2. **Если Custom (LemonAI-based):**
   - Клонировать LemonAI
   - Начать разработку SunaRuntime
   - Создать Docker image для sandbox

### Следующий месяц

1. **Завершить интеграцию**
2. **A/B тестирование**
3. **Decommission Daytona**

---

## Заключение

**Для быстрого решения проблемы рекомендую SandboxAI:**
- ✅ Бесплатно и open-source
- ✅ Простая интеграция (2-3 недели)
- ✅ Минимальные изменения кода
- ✅ Специально для AI agents
- ✅ Docker-based, легко развернуть

**Для долгосрочной перспективы можно рассмотреть Custom решение на основе LemonAI:**
- ✅ Полный контроль
- ✅ Можно добавить любые функции
- ✅ Нет vendor lock-in
- ⚠️ Больше времени (4-6 недель)
- ⚠️ Нужно поддерживать самостоятельно

**Начните с SandboxAI PoC, чтобы быстро решить проблему, затем можно постепенно мигрировать на Custom решение, если потребуется.**
